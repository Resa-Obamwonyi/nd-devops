version: 2.1
# Define a job to be invoked later in a workflow.
orbs:
  aws-cli: circleci/aws-cli@3.1

jobs:
  # create_infrastructure:
  #   docker:
  #     - image: amazon/aws-cli
  #   steps:
  #     - checkout
  #     - run:
  #         name: "Create Cloudformation stack"
  #         command: aws cloudformation deploy --template-file template.yml --stack-name myStack-${CIRCLE_WORKFLOW_ID:0:5} --region us-east-1

  # Exercise: Config and Deployment
  configure_infrastructure: 
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints: ["b1:ee:af:c7:56:17:9d:75:e3:ba:e5:af:71:22:6d:fe"] 
      - run:
          name: Install Ansible
          command: apk add --update ansible
      - run:
          name: Run Playbook and Configure server
          command: |
            ansible-playbook -i inventory.txt main.yml

# Invoke jobs via workflows
workflows:
  create-infra-workflow:
    jobs:
      # - create_infrastructure
      - configure_infrastructure
